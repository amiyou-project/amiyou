server:
  port: 8091
spring:
  application:
    name: tm-rest-service
  datasource:
    url: jdbc:mysql://localhost/amiyou?serverTimezone=UTC
    username: ea
    password: cs544
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
      
  
 
     
    

